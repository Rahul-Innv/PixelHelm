# Candidate briefs (neutral labels; order fixed for you)

## candidate-1

Direction intent (written before any code existed): "An alpine lift-status board: each trail is a bold tile whose color field you can read from across the parking lot, five seconds to scan the whole wall."

Declared convention breaks (per the pre-registration's Amendment A1, these and ONLY these are the candidate's deliberate breaks; any other convention break you find is undeclared and scores as a mistake, not a signature):
- The status color field dominates each tile — the biggest visual element is the status itself (breaks text-first restraint; at a trailhead you read color fields at distance first).
- Status speaks glyph-first — oversized geometric marks (disc/triangle/cross/dashed ring) carry the primary status language ahead of words (breaks label-first convention; shapes make status color-independent).

Machine-gate outcome: All shipped machine gates GREEN for this candidate: token-contract AA recompute (text>=4.5, graphic>=3.0, light+dark); structural output floor (single main, h1, heading order, meta description); axe-core 0 serious / 0 critical on all 4 rendered cells; no horizontal overflow at 280/320/414px; state-aware contrast (default/hover/focus, both modes) pass; target-size (WCAG 2.5.8) pass; focus-trap not-applicable (no dialog); derived-claims honesty gate pass (9 data blocks scoped); required-content manifest pass. Mode fidelity verified ok for light and dark renders.

## candidate-2

Direction intent (written before any code existed): "One decisive opening: the whole network's answer in plain words on the left, the two things that could change your plan on the right — everything else waits below the fold."

Declared convention breaks (per the pre-registration's Amendment A1, these and ONLY these are the candidate's deliberate breaks; any other convention break you find is undeclared and scores as a mistake, not a signature):
- The opening viewport spends its space on a single typographic verdict (the open-count in plain words) instead of the trail list (breaks the list-first utility convention; the answer, not the data, is the product).
- Two-ink monochrome plus one reserved safety orange — orange spent ONLY on caution/closed/alert marks (breaks the green/amber/red status-triad convention; calm made literal, one alarm color that is never wallpaper).

Machine-gate outcome: All shipped machine gates GREEN for this candidate: token-contract AA recompute (text>=4.5, graphic>=3.0, light+dark); structural output floor (single main, h1, heading order, meta description); axe-core 0 serious / 0 critical on all 4 rendered cells; no horizontal overflow at 280/320/414px; state-aware contrast (default/hover/focus, both modes) pass; target-size (WCAG 2.5.8) pass; focus-trap not-applicable (no dialog); derived-claims honesty gate pass (9 data blocks scoped); required-content manifest pass. Mode fidelity verified ok for light and dark renders.

## candidate-3

Direction intent (written before any code existed): "The ranger's morning logbook: every trail is one ruled ledger line with its status stamped beside it, read top to bottom like the board at the trailhead kiosk."

Declared convention breaks (per the pre-registration's Amendment A1, these and ONLY these are the candidate's deliberate breaks; any other convention break you find is undeclared and scores as a mistake, not a signature):
- No card, panel, or button chrome anywhere — the whole interface is typographic rules, margins, and a paper ground (breaks the card-and-chip dashboard convention; a paper log carries trust through plainness).
- Status is a letterpress-style stamp (uppercase, outlined, faintly rotated) instead of a color pill — color demotes to secondary ink (a stamp reads as an act performed by a person; verified-by-someone made visible).

Machine-gate outcome: All shipped machine gates GREEN for this candidate: token-contract AA recompute (text>=4.5, graphic>=3.0, light+dark); structural output floor (single main, h1, heading order, meta description); axe-core 0 serious / 0 critical on all 4 rendered cells; no horizontal overflow at 280/320/414px; state-aware contrast (default/hover/focus, both modes) pass; target-size (WCAG 2.5.8) pass; focus-trap not-applicable (no dialog); derived-claims honesty gate pass (9 data blocks scoped); required-content manifest pass. Mode fidelity verified ok for light and dark renders.
