# Candidate briefs (neutral labels; order fixed for you)

## candidate-1

Direction intent (written before any code existed): "The pitch is typeset on the product's own paper: the page is one worked Ledgerline invoice — features as line items, the real price as the total, the trial as the payment terms — so you judge the tool by holding the thing it makes for you."

Declared convention breaks (per the pre-registration's declared-breaks convention, these and ONLY these are the candidate's deliberate breaks; any other convention break you find is undeclared and scores as a mistake, not a signature):
- The page is a document artifact, not a landing page — one continuous invoice sheet replaces the hero + feature-sections scaffold entirely (an invoicing tool's most credible demonstration is a competent invoice; the page demonstrates the promise instead of asserting it).
- Persuasion is carried by document furniture only — ruled lines, totals, a terms footer, and a single ink stamp; no badges, cards, gradients, or marketing chrome anywhere (a tradesperson trusts paperwork that looks like honest paperwork).

Machine-gate outcome: All shipped machine gates GREEN for this candidate: token-contract AA recompute (text>=4.5, graphic>=3.0, light+dark); structural output floor (single main, h1, heading order, meta description); axe-core 0 serious / 0 critical on all 4 rendered cells; no horizontal overflow at 280/320/414px; state-aware contrast (default/hover/focus, both modes) pass; target-size (WCAG 2.5.8) pass; focus-trap not-applicable (no dialog); derived-claims honesty gate pass (14 data blocks scoped, incl. the shipped/beta verdict-word binding and the pilot-claim provisional-basis binding, both modes); required-content manifest pass (28 required items + 14 block anchors). Mode fidelity verified ok for light and dark renders.

## candidate-2

Direction intent (written before any code existed): "The page is the quote a good tradesperson gives: the full price, the limits, and the small print set large in the first screenful — because for someone who distrusts subscriptions, the terms are the pitch."

Declared convention breaks (per the pre-registration's declared-breaks convention, these and ONLY these are the candidate's deliberate breaks; any other convention break you find is undeclared and scores as a mistake, not a signature):
- Pricing is the first content section — price, limits, VAT note, and trial terms appear before any feature is described, breaking the price-last SaaS convention (the subscription-skeptic's first question answered before selling anything).
- The small print is set large — the terms landing pages bury (VAT-exclusive note, invoice limits, the QuickBooks BETA status, support hours) are typeset at reading size with weight equal to the sell (surfacing the terms unprompted is how the page earns a distrustful visitor's trial click).

Machine-gate outcome: All shipped machine gates GREEN for this candidate: token-contract AA recompute (text>=4.5, graphic>=3.0, light+dark); structural output floor (single main, h1, heading order, meta description); axe-core 0 serious / 0 critical on all 4 rendered cells; no horizontal overflow at 280/320/414px; state-aware contrast (default/hover/focus, both modes) pass; target-size (WCAG 2.5.8) pass; focus-trap not-applicable (no dialog); derived-claims honesty gate pass (14 data blocks scoped, incl. the shipped/beta verdict-word binding and the pilot-claim provisional-basis binding, both modes); required-content manifest pass (28 required items + 14 block anchors). Mode fidelity verified ok for light and dark renders.

## candidate-3

Direction intent (written before any code existed): "Six plain steps from job done to money put aside: the page walks the tradesperson's own afternoon — photo the job sheet, send from the van, paid off a text — so the promise is read as a sequence you already live."

Declared convention breaks (per the pre-registration's declared-breaks convention, these and ONLY these are the candidate's deliberate breaks; any other convention break you find is undeclared and scores as a mistake, not a signature):
- The opening viewport is step one of a story, not a value proposition — "Job's done. Photo the job sheet." replaces the headline + subhead + product-shot hero (the audience decides from recognition, not claims).
- Oversized step numerals are the primary graphic language — display-scale 1–6 set in the accent ink, breaking the icon-tile feature-grid convention (numbered steps are how trade method statements read; counting from job to paid IS the promise made visible).

Machine-gate outcome: All shipped machine gates GREEN for this candidate: token-contract AA recompute (text>=4.5, graphic>=3.0, light+dark); structural output floor (single main, h1, heading order, meta description); axe-core 0 serious / 0 critical on all 4 rendered cells; no horizontal overflow at 280/320/414px; state-aware contrast (default/hover/focus, both modes) pass; target-size (WCAG 2.5.8) pass; focus-trap not-applicable (no dialog); derived-claims honesty gate pass (14 data blocks scoped, incl. the shipped/beta verdict-word binding and the pilot-claim provisional-basis binding, both modes); required-content manifest pass (28 required items + 14 block anchors). Mode fidelity verified ok for light and dark renders.
