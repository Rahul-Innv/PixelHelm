# E3(saas) run report — 2026-07-26 (sealed Ledgerline SaaS-marketing brief)

**E3(saas) verdict: the complete evidenced loop ran end-to-end on an
unfamiliar archetype with zero improvisation outside the documented process.**
Every stage has committed artifacts; the panel verdict and run records
validate through `records.mjs` (a panel whose record does not validate did
not happen — these do). All judged scores below are **system-esteem**
(RF-1/RF-4): conformance to the pre-registered rubric as judged by model
jurors, not user-outcome evidence — and per the sealed rubric's
**Amendment C1**, every 9+ median in this report reads as **"strong,
owner-verify"**, never as achieved distinctive excellence.

## Loop trail (each stage committed as it happened)

1. **Admission** — ChoiceGate continuation admitted against the accepted
   public v0.2.1 authority: `admitted: true`, edition `pixelhelm-lite`
   (`admission/`, sha256 `53b006a5…f7c1`). `admit_choicegate.py` untouched.
2. **Ground** — register, content authority + trap contract (14 tagged
   blocks, LIVE/BETA status-word discipline), machine floor, registry state,
   prior-state read incl. the E1 calibration lesson (`ground/GROUND.md`).
3. **Directions** — three arms with frozen one-sentence intents + A1
   declared-breaks blocks, committed before any candidate code existed
   (`directions/DIRECTIONS.md`): `worked-invoice` (demonstrate),
   `driveway-to-paid` (narrate), `fair-quote` (disclose).
4. **Candidates** — all three full arms committed (winner and losers) with
   injector + honesty configs (`project/arms/`, `project/honesty/`). One
   pre-freeze palette tuning pass (declared-allowed): rule tokens in two arms
   adjusted after check-token-contracts read 2.43–2.77 < 3.0; recorded in
   the git log; no render or score existed at tuning time.
5. **Render** — 12 cells (desktop/mobile × light/dark), mode-fidelity ok on
   all, axe 0 serious / 0 critical on all (`project/renders/`). L-081 page
   identity asserted per arm (title + `data-arm`) before any measurement
   (`project/gates/identity.json`).
6. **Gates** — every shipped HARD gate green for all three arms:
   token-contract AA recompute, structural output floor, static contrast,
   `verify_responsive` (280/320/414), `verify_states` (both modes),
   `verify_targetsize`, `verify_focustrap` (no dialog — not-applicable pass),
   honesty Gate A (14 blocks scoped; verdict word `LIVE`/positive `shipped`;
   provisional `estimate` binding enforcing the pilot-claim basis; registered
   extra numeric contexts £/hours/seconds/clock) and Gate B (28 required
   items + 14 anti-deletion anchors) (`project/gates/`).
7. **Mutant ritual** — 4/4 planted lies fired (FABRICATION £250,
   ASSOCIATION $2,387, VERDICT beta-stamped-LIVE, MISSING VAT-note) on
   never-committed scratch copies; unmutated arms clean
   (`project/honesty/mutant-ritual/RITUAL.md`).
8. **Panel** — 5 blind model jurors, fresh contexts, shuffled neutral labels
   (seeds 202607561–565 in `jurors/blind-map.json`); inputs exactly per the
   sealed evaluator rule (+ the declared-breaks blocks as criterion 8's
   evidence, the E1-precedent A1 reading). Raw per-juror JSONs:
   `jurors/raw/`. Aggregation per the registered rule:
   `jurors/panel-results.json`.
9. **Repair** — not triggered: no Layer-1 failure and no [Blocker]; panel
   advisory findings recorded in the verdict's constraints (the loop spends
   iterations only on machine failures/blockers).
10. **Records** — `pixelhelm/judge-verdict@1` + ledger line +
    `pixelhelm/run@1` written and validated via `records.mjs`
    (`project/.pixelhelm/`). `signoff@1` deliberately NOT written: the owner
    has not yet spoken on the artifact; sign-off happens at owner review.

## Panel result (system-esteem; C1 reading applies throughout)

| arm | overall median | min criterion median | criteria at 9+ (C1: strong, owner-verify) |
|---|---|---|---|
| **worked-invoice** (winner) | **9.0** | 8 | 6 of 10 |
| fair-quote | 9.0 | 7 | 6 of 10 |
| driveway-to-paid | 8.0 | 8 | 4 of 10 |

The top two arms tied on the overall median; the winner was decided by E1's
mechanical five-stage tie-break applied unchanged (`jurors/TIEBREAK.md`),
resolving at stage (a): worked-invoice's weakest criterion median (8) beats
fair-quote's weakest (7 — criterion 3, the single-CTA conversion-path
finding). **Registered PASS bar: MET, and tie-invariantly** — gates
precondition green + mutant ritual recorded; no criterion median below 6
(winner's minimum: 8); overall median 9.0 ≥ 8. Per Amendment C1 the 9.0
medians are recorded as "strong, owner-verify" — the owner's verification
pass is the binding read at the top of the scale; nothing here claims
achieved distinctive excellence.

## Registered thresholds → measured (no reframing)

| registered item | threshold (sealed) | measured | verdict |
|---|---|---|---|
| Floor battery precondition | every shipped HARD gate green ×3 arms + 4-mutant ritual recorded | all green (12 gate outputs ×3 arms); 4/4 mutants fired, unmutated arms clean | PASS |
| Rubric clause 2 | no criterion median below 6 (winner) | winner min 8 (all arms' min ≥ 7) | PASS |
| Rubric clause 3 | overall median ≥ 8 (winner) | 9.0 (C1: strong, owner-verify) | PASS |
| Falsification: register regression | output regresses to the utility register (status-page voice selling software) | all five jurors scored voice/register 8–9 on every arm; no juror observed utility-register regression; archetype-specific forms (invoice conceit, quote sheet, step narrative) carried the register | NOT TRIGGERED |
| Falsification: floor-failure clustering | floor failures cluster here vs the utility run (E1) | E1: 0 HARD-gate failures ×3 arms; E3(saas): 0 HARD-gate failures ×3 arms | NOT TRIGGERED |

**E3(saas) brief-level success criterion met:** the output clears the floor
battery AND the pre-committed rubric bar with archetype criteria explicitly
scored (Section B criteria 7–10 individually judged by all five jurors).
No E2 divergence measurement ran — that was E1-only by design; divergence
discipline in E3 was qualitative (declared motif budgets and palette worlds,
honored per juror observation).

## Deviations & execution decisions (complete list)

- **Winner tie-break** — not registered; E1's mechanical hierarchy adopted
  unchanged before computing whom it favors; resolved at stage (a)
  (`jurors/TIEBREAK.md`).
- **"(fictional)" parentheticals** — recorded pre-render in GROUND: the
  sealed data's `(fictional)` / `(fictional persona)` markers are
  dataset-synthetic metadata (the file's own `sealedNote`), not page copy;
  pages print the attribution/location without the marker.
- **Status-word discipline** — the sealed data has no display-word contract;
  GROUND froze uppercase `LIVE`/`BETA` as integration status words so the
  false-verdict gate binds mechanically (E1's OPEN/CAUTION/CLOSED pattern).
- **Gate A extra numeric contexts** — the shipped gate's money regex is
  $-anchored; the sealed data is GBP. Registered extra contexts (£, hours,
  seconds, clock-time) added in the committed config BEFORE any gate ran;
  the association check remains $-scoped as shipped (m2 exercised it).
- **Pre-freeze palette tuning** — declared-allowed; used once, before any
  render/score existed (rule-token contrast, two arms).
- **No extreme-content render variants** — substitutions would inject
  non-sealed strings; the sealed data is the only content authority.
- **Committed-artifact path hygiene** — all committed gate/ritual outputs
  and stderr captures carry `%USERPROFILE%` markers in place of
  user-absolute paths (E1 lesson applied from the start; no gate content
  changed).

## What may be claimed

The complete artifact trail exists on a second, unfamiliar archetype; gates
fire correctly on archetype-specific traps (beta-as-shipped, VAT-note
deletion — mutant ritual recorded); the panel produced a validated verdict
with recorded per-juror scores — **E3's transfer claim for THIS archetype is
supported: the loop, not the artifact, transferred**. The winner clears the
registered PASS bar (system-esteem, C1-read). Scope honesty: this is one of
E3's three archetypes — commerce and editorial remain unrun, and passing all
three covers four archetypes total; portfolio and product-application stay
untested and unclaimable. No user-outcome claim is made or permitted from
this run (E5's job). Owner sign-off (and any promotion) remains pending and
owner-gated; per Amendment C1 the owner's verification pass is the binding
read on every 9.0 in this report.
